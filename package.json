{
  "name": "giglega",
  "version": "1.0.0",
  "description": "GigLega platform",
  "scripts": {
    "start": "parcel src/*.html",
    "build": "parcel build src/*.html",
    "lint": "eslint src --ext .js,.mjs"
  },
  "dependencies": {
    "parcel": "^2.8.3"
  }
}